# Inventory Management System

>Using python with Flask Framework Micro Wahala
In this Application customer can..............................................................................................
## Customer can ADD/EDIT/DELETE the details in system

# HOW TO RUN THIS:


	First install a flask framework using cmd>pip install Flask  //for windows users
	
	After that download a clone file using git(url) in cmd 

# SET a flask environment in a cmd current library:

	cmd>set FLASK_APP=home.py //window user
	cmd>flask run (if debug is true so not run this cmd instant of that run home.py directly)
	$ export FLASK_APP=home.py //Linux User & Mac Users
	$ flask run
	Now Application are run on this localhost = http://127.0.0.1:5000/


<!-- -->
	
># Built By @thisisazeez
	
	
